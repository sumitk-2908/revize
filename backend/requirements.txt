# Web Server & API Framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Database & Data Handling
python-multipart>=0.0.12
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Authentication & Security
supabase>=2.0.0
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0
httpx>=0.24.0

# Rate Limiting & Security (NEW)
slowapi>=0.1.9
sentry-sdk[fastapi]>=1.39.0

# Standard utility for system paths
email-validator>=2.0.0

# PDF Processing
PyMuPDF>=1.23.0

# OCR fallback so scanned PDFs and image uploads are still full-text searchable.
# Needs the Tesseract binary on the host, which pip cannot supply:
#   Debian/Ubuntu: apt-get install tesseract-ocr
#   macOS:         brew install tesseract
# Without it the OCR branch logs a warning and the file is stored without
# searchable content rather than failing the upload.
pytesseract>=0.3.10
Pillow>=10.0.0

boto3==1.34.*